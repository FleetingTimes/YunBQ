# 前端环境变量模板（.env.example）
# 目的：避免将真实后端地址等环境参数提交到版本库。
# 使用方法：复制为 .env.development 或 .env.production，并设置实际值。

# API 基址（必须包含 /api 前缀）
# 本地联调示例：后端监听 6639 端口
VITE_API_BASE=http://localhost:6639/api

# 生产/公网示例：通过 HTTPS 与自定义域名（请替换你的域名）
# VITE_API_BASE=https://api.example.com/api

# —— 联网测试（开发服务器通过隧道/反向代理暴露到外网） ——
# Vite Dev Server 配置（供 vite.config.js 读取；非必须，仅在需要 Cloudflare Tunnel 等场景使用）
# 允许的外部主机白名单：设置为逗号分隔域名，或 true 表示放开全部（不推荐公网）
# DEV_ALLOWED_HOSTS=app.example.com,preview.example.com
# 静态资源绝对来源（origin）：你的外网前端域名
# DEV_ORIGIN=https://app.example.com
# HMR（热更新）参数：host/协议(client 连接协议)/client 端口
# DEV_HMR_HOST=app.example.com
# DEV_HMR_PROTOCOL=wss
# DEV_HMR_CLIENT_PORT=443
