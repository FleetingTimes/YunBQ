# YunBQ Docker Compose 环境变量示例（请复制为 docker-compose.env 并填写真实值）

# 基础端口（宿主映射）
EXPOSE_PORT=6639
MYSQL_PORT=3306
REDIS_PORT=6379

# MySQL 基础配置（首次部署请使用强密码）
MYSQL_DATABASE=yunbq
MYSQL_USER=yunbq
MYSQL_PASSWORD=please_change_mysql_user_password
MYSQL_ROOT_PASSWORD=please_change_mysql_root_password

# 后端数据库与缓存配置
SPRING_DATASOURCE_URL=jdbc:mysql://mysql:3306/yunbq?useUnicode=true&characterEncoding=utf8&serverTimezone=UTC&useSSL=false
SPRING_DATASOURCE_USERNAME=yunbq
SPRING_DATASOURCE_PASSWORD=please_change_mysql_user_password
SPRING_DATA_REDIS_HOST=redis
SPRING_DATA_REDIS_PORT=6379

# JWT 配置（必须替换为随机且足够长的密钥）
JWT_SECRET=please_replace_with_a_strong_random_secret
JWT_ISSUER=com.linaa.shiyan
JWT_EXPIRE_MINUTES=1440

# 自动建表控制：
// 首次部署设为 always，建表成功后改为 never；若已手动建表则保持 never
SPRING_SQL_INIT_MODE=never
# 如需首次自动建表：
# SPRING_SQL_INIT_MODE=always

# 说明：前端访问后端 API 的地址由打包前设置 VITE_API_BASE 控制，此处无需设置。
